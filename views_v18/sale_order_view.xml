<?xml version="1.0" encoding="UTF-8"?>
<!-- Odoo 18 syntax: list instead of tree, direct invisible="expr" not attrs -->
<odoo>
    <data>

        <!-- ── Sale Order Form: Marketplace fields (Odoo 18) ───────────── -->
        <record id="bigseller_sale_order_form_inherit" model="ir.ui.view">
            <field name="name">sale.order.form.bigseller.inherit</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='commitment_date']" position="after">
                    <field name="buyer_designed_logistics"/>
                </xpath>

                <xpath expr="//group[@name='sale_header']" position="after">
                    <group string="Marketplace" name="marketplace_group"
                           invisible="not mp_marketplace">
                        <group>
                            <field name="mp_marketplace" readonly="1"/>
                            <field name="mp_status" readonly="1"/>
                            <field name="bigseller_shop_name" readonly="1"
                                   invisible="not bigseller_shop_name"/>
                        </group>
                        <group>
                            <field name="mp_last_update" readonly="1"/>
                            <field name="bigseller_order_id" readonly="1"
                                   invisible="not bigseller_order_id"/>
                            <field name="bigseller_platform_order_id" readonly="1"
                                   invisible="not bigseller_platform_order_id"/>
                            <field name="import_session_id" readonly="1"
                                   invisible="not import_session_id"/>
                        </group>
                    </group>
                </xpath>

                <xpath expr="//notebook" position="inside">
                    <page string="MP Status" name="mp_status_tab"
                          invisible="not mp_marketplace">
                        <field name="mp_status_history_ids" nolabel="1">
                            <list create="0" delete="0">
                                <field name="update_date"/>
                                <field name="marketplace"/>
                                <field name="bigseller_status"/>
                                <field name="mp_status"/>
                                <field name="odoo_action"/>
                                <field name="notes"/>
                            </list>
                        </field>
                    </page>
                </xpath>

            </field>
        </record>

    </data>
</odoo>
